import{useEffect}from'react';export function useEntranceMotion(){useEffect(()=>{const root=document.documentElement;if(matchMedia('(prefers-reduced-motion: reduce)').matches)return;root.dataset.motion='on';const groups=[...document.querySelectorAll<HTMLElement>('[data-motion-group]')];const reveal=(g:HTMLElement)=>{g.dataset.visible='true';g.querySelectorAll<HTMLElement>('[data-motion-item]').forEach((el,i)=>el.style.setProperty('--i',String(i)))};const observer=new IntersectionObserver(entries=>entries.filter(e=>e.isIntersecting).sort((a,b)=>groups.indexOf(a.target as HTMLElement)-groups.indexOf(b.target as HTMLElement)).forEach(e=>{reveal(e.target as HTMLElement);observer.unobserve(e.target)}),{threshold:.08,rootMargin:'0px 0px -8%'});requestAnimationFrame(()=>requestAnimationFrame(()=>groups.forEach(g=>observer.observe(g))));return()=>{observer.disconnect();delete root.dataset.motion}},[])}
