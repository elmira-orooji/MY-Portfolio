# Engineer portfolio

Independent React/TypeScript portfolio project.

Before publishing, replace `Your Name`, initials, resume/social/email links, placeholder projects, and skills marked `verify`.

```bash
npm install
npm run dev
```
